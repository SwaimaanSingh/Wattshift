node_modules
dist
dist-ssr
.env
.env.local
*.local
.DS_Store
.vite

# Vercel CLI project linkage (contains project and org IDs).
.vercel

# Extracted text from real customer bills — never commit these.
scripts/sample-text/
